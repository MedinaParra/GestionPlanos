# La aplicación no requiere claves API, Firebase ni secretos dentro del APK.
# La autorización se realiza con el cliente OAuth Android registrado en Google Cloud.
# Los permisos, perfiles y archivos del flujo se almacenan directamente en Google Drive.
